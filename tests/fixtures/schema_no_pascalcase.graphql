enum Color { red, green }

type zooKeeper implements BaseOOI & BaseObject {
  object_type: String!
  primary_key(natural_key: [String] = ["name"]): ID!
  human_readable(format: String = "{name} pets {pet_name}"): String!
  name: String!
  pet: Animal!
}

type animal implements BaseOOI & BaseObject {
  object_type: String!
  primary_key(natural_key: [String] = ["name"]): ID!
  human_readable(format: String = "Hello: {name}"): String!
  name: String!
  color: Color!
}
