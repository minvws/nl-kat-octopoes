# Native Kat
interface BaseObject {
   primary_key: ID!
}

interface BaseOOI implements BaseObject {
   primary_key: ID!
   human_readable: String!
}

type Test implements BaseOOI & BaseObject {
   primary_key(natural_key: [String] = ["test"]): ID!
   human_readable: String!
   test: String!
}

type Query implements BaseOOI & BaseObject {
    test: Test!
}